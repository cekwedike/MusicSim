# ==============================================
# OPTION 1: LOCAL SETUP (Traditional)
# ==============================================

# Database Configuration (Local PostgreSQL)
DATABASE_URL=postgresql://localhost:5432/musicsim
DB_HOST=localhost
DB_PORT=5432
DB_NAME=musicsim
DB_USER=postgres
DB_PASSWORD=

# Google OAuth Configuration (Manual Setup)
# Get these from Google Cloud Console (https://console.cloud.google.com/)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3001/api/auth/google/callback

# ==============================================
# OPTION 2: SUPABASE SETUP (Recommended)
# ==============================================

# Supabase Database Configuration
# Get these from: Supabase Dashboard → Settings → Database
# DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres

# Supabase Authentication
# Get these from: Supabase Dashboard → Settings → API
# SUPABASE_URL=https://xxxxx.supabase.co
# SUPABASE_SERVICE_KEY=your-service-role-key-here

# Note: With Supabase, Google OAuth is configured in the dashboard
# You don't need GOOGLE_CLIENT_ID/SECRET here

# ==============================================
# COMMON CONFIGURATION
# ==============================================

# Server Configuration
PORT=3001
NODE_ENV=development

# Frontend Configuration (for CORS)
FRONTEND_URL=http://localhost:5173

# Migration API Secret (for remote database migrations)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
MIGRATION_SECRET=change-me-in-production

# Security Notes:
# 1. Use environment-specific database credentials
# 2. Never commit this file to version control
# 3. Consider using a key management service in production
# 4. Set up Google OAuth credentials at https://console.cloud.google.com/