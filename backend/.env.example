# Database Configuration
DATABASE_URL=postgresql://localhost:5432/musicsim
DB_HOST=localhost
DB_PORT=5432
DB_NAME=musicsim
DB_USER=postgres
DB_PASSWORD=

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-please

# Server Configuration
PORT=3001
NODE_ENV=development

# Frontend Configuration (for CORS)
FRONTEND_URL=http://localhost:5173

# Google OAuth Configuration
# Get these from Google Cloud Console (https://console.cloud.google.com/)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3001/api/auth/google/callback

# Security Notes:
# 1. Generate a strong random JWT_SECRET for production
# 2. Use environment-specific database credentials
# 3. Never commit this file to version control
# 4. Consider using a key management service in production
# 5. Set up Google OAuth credentials at https://console.cloud.google.com/

# Example strong JWT_SECRET generation (use one of these methods):
# openssl rand -base64 64
# node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"