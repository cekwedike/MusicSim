{
  "name": "musicsim-backend",
  "version": "1.0.0",
  "description": "Backend server for MusicSim game",
  "main": "server.js",
  "scripts": {
    "start": "npm run migrate && node server.js",
    "start:no-migrate": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo Running backend tests... && node server-test.js",
    "test:ci": "echo Running CI backend tests... && node ci-test.js",
    "test:dev": "nodemon server-test.js",
    "lint": "echo Backend lint: No linter configured yet",
    "lint:fix": "echo Backend lint fix: No linter configured yet",
    "db:init": "node scripts/initDatabase.js",
    "db:reset": "node scripts/resetDatabase.js",
    "migrate": "node migrations/migrate.js",
    "security:audit": "npm audit",
    "deps:check": "npm outdated"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.79.0",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "passport": "^0.7.0",
    "passport-google-oauth20": "^2.0.0",
    "pg": "^8.11.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.35.0",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "rimraf": "^6.1.0"
  }
}
