// InstallPrompt removed — use InstallBanner instead.
// This file intentionally exports a no-op component so legacy imports (if any) won't break.
import React from 'react';

const InstallPrompt: React.FC = () => null;

export default InstallPrompt;